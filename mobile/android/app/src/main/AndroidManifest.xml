<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Voice messages. The recording is uploaded to the clinic's own server,
         stored with the conversation, and transcribed there so the assistant
         can answer it and the triage rules can assess it. Audio does leave the
         phone — this note used to say otherwise, from when the mic only
         dictated on-device, and that is no longer how it works. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Photographs of feet, prescriptions and reports (image_picker). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Optional biometric app lock (local_auth). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Local notifications (appointment/clinic updates). Runtime-requested on
         Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Medication reminders are exact alarms so they fire on the minute rather
         than whenever Doze next wakes.

         flutter_local_notifications declares only VIBRATE and POST_NOTIFICATIONS
         in its own manifest — everything an exact, reboot-surviving alarm needs
         has to be declared here. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Lets the app ask to be exempted from battery optimization, so aggressive
         OEMs (MIUI/Oppo/Vivo) don't kill the scheduled dose alarms. Requested
         once, in-app, with an explanation — never silently. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Android 11+ package visibility: required for speech_to_text to see
         the system speech recogniser. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <!--
          Package visibility for the dialler. From Android 11 an app cannot see
          which apps handle an intent unless it says which ones it asks about,
          and without this `canLaunchUrl` on a tel: URI answers false on every
          modern handset — including for the emergency card's "Call the clinic"
          button, which is the one button in this app that must never quietly
          do nothing.
        -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>

    <application
        android:label="MedPin"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Channel FCM falls back to when it builds the notification itself
             (app backgrounded or killed). Without this, a background call ring
             or clinic alert can be dropped on Android O+ for want of a channel.
             Must match the id NotificationService creates on the device. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="clinq_updates" />
        <!-- flutter_local_notifications' own manifest declares ONLY permissions
             (checked in v18.0.1) — it contributes no receivers. They have to be
             declared here.

             Without them the alarm fires, Android looks for
             ScheduledNotificationReceiver, finds nothing registered and drops
             the broadcast: no notification, no sound, and no error anywhere.
             Every medicine reminder failed silently this way. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
        <!-- Re-arms every scheduled reminder after a reboot or an app update;
             alarms do not survive either on their own. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
